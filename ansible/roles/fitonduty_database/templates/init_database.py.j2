#!/usr/bin/env python3
"""
Database initialization script for {{ environment }} environment
Generated by Ansible template
"""

import os
import sys
import argparse
from sqlalchemy import create_engine, text

def main():
    db_url = "postgresql://{{ fitonduty_admin_user }}:{{ vault_admin_password }}@localhost:5432/{{ fitonduty_db_name }}"
    
    # Your existing init_db.py logic here, but templated for the environment
    # This would include all the table creation, seeding, etc.
    
    print(f"Database {{ fitonduty_db_name }} initialized successfully for {{ environment }}")

if __name__ == "__main__":
    main()